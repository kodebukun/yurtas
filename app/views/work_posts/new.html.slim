.my-4
  = render partial: "shared/error_message", locals: {content: @post}
  = render partial: "form", locals: {title: "新規投稿（#{@work.label}）", path: work_posts_path, work_id: @work.id}
  .nav.justify-content-end
    = link_to "#{@work.label}の掲示板へ", work_posts_path(work_id: @work.id), class:"nav-link"
